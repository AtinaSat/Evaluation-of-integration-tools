This folder contains the script to create filtered dataset using the raw hepatocellular carcinoma dataset and the filtered dataset (.RData)

The folders, named after the meta-dimensional tool, contain the scripts to analyse the filtered dataset using the tool
